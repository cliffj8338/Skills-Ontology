/**
 * src/ui/icons.js
 * bpIcon() SVG icon system, icon registry, SVG hydration pass
 *
 * STATUS: STUB — not yet migrated
 * MONOLITH LINES: 2376–2520
 *
 * Dependencies:
 *   (none — leaf module)
 *
 * Exports:
 *   bpIcon
 *   hydrateIcons
 */

// TODO: Add imports when migrating


// ─── STUB EXPORTS ────────────────────────────────────────────────────────────
// Replace each with the real implementation during migration.
// Throwing on call ensures broken migrations surface immediately rather than silently.

export function bpIcon() { throw new Error('bpIcon not yet migrated'); }
export function hydrateIcons() { throw new Error('hydrateIcons not yet migrated'); }
