/**
 * src/ui/nav.js
 * switchView(), profile dropdown, overflow menu, filter panel, feedback modal
 *
 * STATUS: STUB — not yet migrated
 * MONOLITH LINES: 24954 (switchView), 46380–46840
 *
 * Dependencies:
 *   ../core/firebase.js
 *   ../core/analytics.js
 *   ../ui/icons.js
 *
 * Exports:
 *   switchView
 *   initNav
 *   openProfileDropdown
 *   toggleFilterPanel
 */

// TODO: Add imports when migrating
// import { ... } from '../core/firebase.js';
// import { ... } from '../core/analytics.js';
// import { ... } from '../ui/icons.js';

// ─── STUB EXPORTS ────────────────────────────────────────────────────────────
// Replace each with the real implementation during migration.
// Throwing on call ensures broken migrations surface immediately rather than silently.

export function switchView() { throw new Error('switchView not yet migrated'); }
export function initNav() { throw new Error('initNav not yet migrated'); }
export function openProfileDropdown() { throw new Error('openProfileDropdown not yet migrated'); }
export function toggleFilterPanel() { throw new Error('toggleFilterPanel not yet migrated'); }
