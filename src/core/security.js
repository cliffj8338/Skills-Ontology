/**
 * src/core/security.js
 * HTML sanitization, safe JSON parse, localStorage wrappers, URL sanitizer
 *
 * STATUS: STUB — not yet migrated
 * MONOLITH LINES: 2067–2193
 *
 * Dependencies:
 *   (none — leaf module)
 *
 * Exports:
 *   escapeHtml
 *   safeParse
 *   safeGet
 *   safeSet
 *   safeRemove
 *   sanitizeUrl
 *   validateImport
 */

// TODO: Add imports when migrating


// ─── STUB EXPORTS ────────────────────────────────────────────────────────────
// Replace each with the real implementation during migration.
// Throwing on call ensures broken migrations surface immediately rather than silently.

export function escapeHtml() { throw new Error('escapeHtml not yet migrated'); }
export function safeParse() { throw new Error('safeParse not yet migrated'); }
export function safeGet() { throw new Error('safeGet not yet migrated'); }
export function safeSet() { throw new Error('safeSet not yet migrated'); }
export function safeRemove() { throw new Error('safeRemove not yet migrated'); }
export function sanitizeUrl() { throw new Error('sanitizeUrl not yet migrated'); }
export function validateImport() { throw new Error('validateImport not yet migrated'); }
