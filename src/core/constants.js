/**
 * src/core/constants.js
 * App-wide constants: version, skill caps, job schema version, dev stats
 *
 * STATUS: STUB — not yet migrated
 * MONOLITH LINES: 1723–1858
 *
 * Dependencies:
 *   (none — leaf module)
 *
 * Exports:
 *   BP_VERSION
 *   BP_BUILD
 *   JOB_SCHEMA_VERSION
 *   SKILL_CAP
 *   SKILL_HARD_CAP
 *   devStats
 */

// TODO: Add imports when migrating


// ─── STUB EXPORTS ────────────────────────────────────────────────────────────
// Replace each with the real implementation during migration.
// Throwing on call ensures broken migrations surface immediately rather than silently.

export function BP_VERSION() { throw new Error('BP_VERSION not yet migrated'); }
export function BP_BUILD() { throw new Error('BP_BUILD not yet migrated'); }
export function JOB_SCHEMA_VERSION() { throw new Error('JOB_SCHEMA_VERSION not yet migrated'); }
export function SKILL_CAP() { throw new Error('SKILL_CAP not yet migrated'); }
export function SKILL_HARD_CAP() { throw new Error('SKILL_HARD_CAP not yet migrated'); }
export function devStats() { throw new Error('devStats not yet migrated'); }
