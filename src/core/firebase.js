/**
 * src/core/firebase.js
 * Firebase init, auth state, Firestore persistence, sign-in/out, admin role check
 *
 * STATUS: STUB — not yet migrated
 * MONOLITH LINES: 2521–3097
 *
 * Dependencies:
 *   ./constants.js
 *   ./security.js
 *   ./analytics.js
 *
 * Exports:
 *   fbApp
 *   fbAuth
 *   fbDb
 *   fbUser
 *   fbIsAdmin
 *   showcaseMode
 *   authReady
 *   saveToFirestore
 *   loadFromFirestore
 */

// TODO: Add imports when migrating
// import { ... } from './constants.js';
// import { ... } from './security.js';
// import { ... } from './analytics.js';

// ─── STUB EXPORTS ────────────────────────────────────────────────────────────
// Replace each with the real implementation during migration.
// Throwing on call ensures broken migrations surface immediately rather than silently.

export function fbApp() { throw new Error('fbApp not yet migrated'); }
export function fbAuth() { throw new Error('fbAuth not yet migrated'); }
export function fbDb() { throw new Error('fbDb not yet migrated'); }
export function fbUser() { throw new Error('fbUser not yet migrated'); }
export function fbIsAdmin() { throw new Error('fbIsAdmin not yet migrated'); }
export function showcaseMode() { throw new Error('showcaseMode not yet migrated'); }
export function authReady() { throw new Error('authReady not yet migrated'); }
export function saveToFirestore() { throw new Error('saveToFirestore not yet migrated'); }
export function loadFromFirestore() { throw new Error('loadFromFirestore not yet migrated'); }
