/**
 * src/core/analytics.js
 * Site analytics tracker, bpTracker, analytics event logging, session tracking
 *
 * STATUS: STUB — not yet migrated
 * MONOLITH LINES: 2195–2346
 *
 * Dependencies:
 *   ./constants.js
 *
 * Exports:
 *   bpTracker
 *   logAnalyticsEvent
 *   initAnalytics
 */

// TODO: Add imports when migrating
// import { ... } from './constants.js';

// ─── STUB EXPORTS ────────────────────────────────────────────────────────────
// Replace each with the real implementation during migration.
// Throwing on call ensures broken migrations surface immediately rather than silently.

export function bpTracker() { throw new Error('bpTracker not yet migrated'); }
export function logAnalyticsEvent() { throw new Error('logAnalyticsEvent not yet migrated'); }
export function initAnalytics() { throw new Error('initAnalytics not yet migrated'); }
