/**
 * src/engine/skill-library.js
 * Skill library loader (ESCO/O*NET/Lightcast), skill blocklist, synonym lookup
 *
 * STATUS: STUB — not yet migrated
 * MONOLITH LINES: 17752–18481
 *
 * Dependencies:
 *   ../core/security.js
 *
 * Exports:
 *   loadSkillLibrary
 *   findSkill
 *   normalizeSkillName
 *   skillBlocklist
 *   synonymLookup
 */

// TODO: Add imports when migrating
// import { ... } from '../core/security.js';

// ─── STUB EXPORTS ────────────────────────────────────────────────────────────
// Replace each with the real implementation during migration.
// Throwing on call ensures broken migrations surface immediately rather than silently.

export function loadSkillLibrary() { throw new Error('loadSkillLibrary not yet migrated'); }
export function findSkill() { throw new Error('findSkill not yet migrated'); }
export function normalizeSkillName() { throw new Error('normalizeSkillName not yet migrated'); }
export function skillBlocklist() { throw new Error('skillBlocklist not yet migrated'); }
export function synonymLookup() { throw new Error('synonymLookup not yet migrated'); }
