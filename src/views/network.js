/**
 * src/views/network.js
 * D3 skill network graph, network overlays (job/match/values), job selector widget
 *
 * STATUS: STUB — not yet migrated
 * MONOLITH LINES: 22489–25732
 *
 * Dependencies:
 *   ../core/security.js
 *   ../core/analytics.js
 *   ../ui/icons.js
 *
 * Exports:
 *   initNetwork
 *   renderNetwork
 *   renderJobOverlay
 *   renderMatchOverlay
 *   renderValuesNetwork
 */

// TODO: Add imports when migrating
// import { ... } from '../core/security.js';
// import { ... } from '../core/analytics.js';
// import { ... } from '../ui/icons.js';

// ─── STUB EXPORTS ────────────────────────────────────────────────────────────
// Replace each with the real implementation during migration.
// Throwing on call ensures broken migrations surface immediately rather than silently.

export function initNetwork() { throw new Error('initNetwork not yet migrated'); }
export function renderNetwork() { throw new Error('renderNetwork not yet migrated'); }
export function renderJobOverlay() { throw new Error('renderJobOverlay not yet migrated'); }
export function renderMatchOverlay() { throw new Error('renderMatchOverlay not yet migrated'); }
export function renderValuesNetwork() { throw new Error('renderValuesNetwork not yet migrated'); }
