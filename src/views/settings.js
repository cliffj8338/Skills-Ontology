/**
 * src/views/settings.js
 * Settings system, experience tab, privacy & data, consent & sharing, data export
 *
 * STATUS: STUB — not yet migrated
 * MONOLITH LINES: 40727–42901
 *
 * Dependencies:
 *   ../core/firebase.js
 *   ../core/security.js
 *   ../ui/icons.js
 *   ../ui/toast.js
 *
 * Exports:
 *   initSettings
 *   renderSettings
 *   renderExperience
 *   renderPrivacy
 *   exportUserData
 */

// TODO: Add imports when migrating
// import { ... } from '../core/firebase.js';
// import { ... } from '../core/security.js';
// import { ... } from '../ui/icons.js';
// import { ... } from '../ui/toast.js';

// ─── STUB EXPORTS ────────────────────────────────────────────────────────────
// Replace each with the real implementation during migration.
// Throwing on call ensures broken migrations surface immediately rather than silently.

export function initSettings() { throw new Error('initSettings not yet migrated'); }
export function renderSettings() { throw new Error('renderSettings not yet migrated'); }
export function renderExperience() { throw new Error('renderExperience not yet migrated'); }
export function renderPrivacy() { throw new Error('renderPrivacy not yet migrated'); }
export function exportUserData() { throw new Error('exportUserData not yet migrated'); }
